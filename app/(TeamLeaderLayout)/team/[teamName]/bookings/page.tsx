/**
 * This page is now handled by TeamLeaderViewShell in the layout.
 * The shell renders the correct view based on pathname, so this file
 * serves as a fallback for direct URL access / SEO crawlers.
 */
export default function TeamBookingsPage() {
  return null; // Rendered by TeamLeaderViewShell
}
